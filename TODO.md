# TODO

这个文件用来记录当前版本最值得继续推进的收尾事项，方便按优先级持续迭代。

修改说明：先把“可以暂时收一版”前最关键的任务拆成 `P0 / P1 / P2`，
这样后面无论是自己开发还是给 GitHub 读者看，节奏都会更清楚。

## P0

修改说明：当前 `P0` 已经收完，如果按“先完成核心闭环再收最终版”的标准，这一版已经可以作为阶段性最终版。

- [x] 补完工具参数 schema
  - [x] 为现有内置工具补齐明确的参数类型、必填项、默认值、枚举约束
  - [x] 支持范围约束与跨字段语义校验
  - [x] 让 Agent 在工具执行前完成参数校验与归一化
  - [x] 文本模式与原生 `tool calling` 共用同一套参数错误反馈链路
  - [x] 继续补复杂嵌套结构和条件 schema

- [x] 统一工具执行结果协议
  - [x] 引入第一版 `ToolResult(success/content/data/error/meta)` 统一结果对象
  - [x] 让更多工具显式返回结构化结果，而不是只返回字符串
  - [x] 把 `ToolResult.meta / data` 接到日志、工具观察和工具记忆
  - [x] 增加第一版自动重试与降级提示
  - 减少 Agent 直接依赖“工具只返回字符串”的隐式约定
  - 为日志、记忆写入、失败恢复打基础

- [x] 让记忆系统形成稳定闭环
  - [x] 明确哪些内容应该写入 working / episodic / semantic
  - [x] 补去重、低价值记忆过滤
  - [x] 让记忆写入结果带上更可解释的决策日志
  - [x] 继续补长期保留策略
  - [x] 让检索结果也输出更细的解释信息

## P1

- [ ] 稳定上下文工程策略
  - 固化 memory / rag / tool / summary 的优先级与预算策略
  - 继续减少冲突上下文和重复上下文
  - 为超长问题补更稳定的裁剪逻辑



